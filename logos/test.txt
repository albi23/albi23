String ruleTemplate = "rule \"SettlementCloseRules_{id}\"" +
        "when" +
        "    $attributes: SettlementCloseAttributes(" +
        "        workflow == \"{workflow}\", " +
        "        settlementStatus == \"{settlementStatus}\", " +
        "        settlementCancelStatus == \"{settlementCancelStatus}\")" +
        "then" +
        "    $outputAttributes.addFiredRules(\"{id}\");" +
        "    $outputAttributes.setUiActive(\"N\");" +
        "end";
